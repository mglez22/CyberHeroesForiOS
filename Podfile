# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CyberHeroe' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CyberHeroe
  pod 'Charts'
  pod 'Firebase'
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  target 'CyberHeroeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CyberHeroeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
